import React from "react";

const initialState = {
  maleLions: 8,
  femaleLions: 18,
  totalLions: 0,
};

export const lionReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
